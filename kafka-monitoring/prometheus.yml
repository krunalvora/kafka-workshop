global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets:
        - localhost:9090

  - job_name: 'kafka'
    static_configs:
      - targets:
        - kafka1:29092
        - kafka2:29092
        - kafka3:29092
